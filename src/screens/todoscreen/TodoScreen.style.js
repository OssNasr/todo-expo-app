import { StyleSheet } from 'react-native';
import { bodyColor } from '../../globals/globalStyles';

export default StyleSheet.create({
    wrapper: {
        flex: 1,
        width: '100%',
        padding: '3%',
        backgroundColor: bodyColor,
    }
});
